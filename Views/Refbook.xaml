<Window x:Class="MedicalRefbook2_0.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MedicalRefbook2_0"
        xmlns:uc="clr-namespace:UControls"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="1*"/>
                <RowDefinition Height="25"/>
            </Grid.RowDefinitions>
            <TabControl Grid.Row="0">
            <TabItem Header="Справочник">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="0.3*"/>
                            <ColumnDefinition Width="0.7*"/>
                        </Grid.ColumnDefinitions>
                        <GridSplitter Grid.Column="0" BorderThickness="3" BorderBrush="DarkGray" VerticalAlignment="Stretch"/>
                        <StackPanel Grid.Column="0">
                            <TextBox Name="SearchBtn" Text="Поиск" Margin="0 0 5 3"/>
                            <TreeView Name="HierarchyTreeView" Margin="0 0 5 3">
                                <TreeView.ItemTemplate>
                                    <HierarchicalDataTemplate ItemsSource="{Binding IndexToType}">
                                        <TextBlock Text="{Binding Path=NameType}" />
                                        <HierarchicalDataTemplate.ItemTemplate>
                                            <DataTemplate>
                                                <TextBlock Text="{Binding Path=ShortName}" MouseLeftButtonDown="TextBlock_MouseLeftButtonDown">
                                                </TextBlock>
                                            </DataTemplate>
                                        </HierarchicalDataTemplate.ItemTemplate>
                                    </HierarchicalDataTemplate>
                                </TreeView.ItemTemplate>
                            </TreeView>
                        </StackPanel>
                        <ScrollViewer HorizontalScrollBarVisibility="Disabled"  Grid.Column="1">
                            <StackPanel>
                                <uc:FormIndexControl ReadOnly="True">
                                    <uc:FormIndexControl.Resources>
                                        <Style TargetType="uc:FormIndexControl">
                                            <Setter Property="ShortName" Value="{Binding DataContext.InfoSelectedIndex[0], RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type Window}}}"/>
                                            <Setter Property="FullName" Value="{Binding DataContext.InfoSelectedIndex[1], RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type Window}}}"/>
                                            <Setter Property="Group" Value="{Binding DataContext.InfoSelectedIndex[2], RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type Window}}}"/>
                                            <Setter Property="Measure" Value="{Binding DataContext.InfoSelectedIndex[3], RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type Window}}}"/>
                                            <Setter Property="IndexFormula" Value="{Binding DataContext.InfoSelectedIndex[4], RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type Window}}}"/>
                                            <Setter Property="Description" Value="{Binding DataContext.InfoSelectedIndex[5], RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type Window}}}"/>
                                            <Setter Property="EquipMeasure" Value="{Binding DataContext.InfoSelectedIndex[6], RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type Window}}}"/>
                                            <Setter Property="Average" Value="{Binding DataContext.InfoSelectedIndex[7], RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type Window}}}"/>
                                            <Setter Property="AdditionalInfo" Value="{Binding DataContext.InfoSelectedIndex[8], RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type Window}}}"/>
                                        </Style>
                                    </uc:FormIndexControl.Resources>
                                </uc:FormIndexControl>
                                <Label Content="Где используется"/>
                                <ItemsControl Name="DependIndicesICtrl" Height="auto" ItemsSource="{Binding Path=UsingIndicesList}">
                                    <ItemsControl.ItemsPanel>
                                        <ItemsPanelTemplate>
                                            <WrapPanel/>
                                        </ItemsPanelTemplate>
                                    </ItemsControl.ItemsPanel>
                                </ItemsControl>
                                <Label Content="От чего зависит" />
                                <ItemsControl Name="UsingIndicesICtrl" Height="auto" ItemsSource="{Binding Path=DependIndicesList}">
                                    <ItemsControl.ItemsPanel>
                                        <ItemsPanelTemplate>
                                            <WrapPanel/>
                                        </ItemsPanelTemplate>
                                    </ItemsControl.ItemsPanel>
                                </ItemsControl>
                            </StackPanel>
                        </ScrollViewer>
                    </Grid>
                </TabItem>
            <TabItem Header="Граф" >

            </TabItem>
            <TabItem Header="Новая запись">

            </TabItem>
            <TabItem Header="Заявки">

            </TabItem>
        </TabControl>
        <Button Name="Settings" Content="Настройки" Grid.Row="1" HorizontalAlignment="Right" Width="100"/>
        </Grid>
    </Grid>
</Window>
